import { Box, Typography } from "@mui/material";

export const TodoList = () => {
  return (
    <Box>
      <Typography variant="h3" component="h3">
        Importante
      </Typography>
    </Box>
  );
};
